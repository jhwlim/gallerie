<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="com.kgitbank.spring.domain.model.mapper.LoginMapper">

	<select id="selectAll" resultType="com.kgitbank.spring.domain.model.LoginVO">
		SELECT * FROM logins
	</select>

	<insert id="createNewLogin">
		INSERT INTO
			logins
		VALUES (
			login_seq.nextval, #{memberSeqId}, sysdate, #{location}, #{ip}
		)
	</insert>

</mapper>